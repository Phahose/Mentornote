<Window x:Class="Mentornote.Desktop.Overlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MentorNote Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Width="{x:Static SystemParameters.PrimaryScreenWidth}"
        Height="{x:Static SystemParameters.PrimaryScreenHeight}">

    <!-- Root grid is transparent; click-through except where we set IsHitTestVisible="True" -->
    <Grid>

        <!-- transparent, click-through background -->
        <Rectangle Fill="Transparent" IsHitTestVisible="False"/>
        
        <Rectangle Stroke="MediumPurple"
               StrokeThickness="10"
               RadiusX="12" RadiusY="12"
               IsHitTestVisible="False"/>
       
        <!-- === Listening Card (top-left) === -->
        <Border Width="300" Height="100"
                CornerRadius="20"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="60,60,0,0"
                Background="#20FFFFFF"    

                IsHitTestVisible="True">
            <StackPanel Margin="20">
                <TextBlock x:Name="RecordingCheck"
                           Text="🎧  Listening..."
                           Foreground="White"
                           FontWeight="Bold"
                           FontSize="16"/>
                <ProgressBar IsIndeterminate="True"
                             Height="4"
                             Foreground="MediumPurple"
                             Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- === Suggestion Card (center-right) === -->
        <Border Width="380"
                CornerRadius="20"
                HorizontalAlignment="Right"
                VerticalAlignment="top"
                Margin="0,150,60,0"
                Background="#15FFFFFF"
               
                IsHitTestVisible="True">
            <StackPanel Margin="20">
                <TextBlock x:Name="RecordedText"
                           Text=""
                           Foreground="white"
                           FontWeight="Bold"
                           FontSize="14"
                           Margin="0,0,0,4"/>
                <TextBlock Text="How is the project coming..."
                           Foreground="MediumPurple"
                           FontSize="18"
                           TextWrapping="Wrap"/>
                
                <TextBlock Text="The project is looking good alignment with the project plan and we are looking to finsh  ahead of the schedule we did have some constrsaints on the cost sand Wala Quit Last week "
                           Foreground="white"
                           FontSize="18"
                           Margin="0,10,0,4"
                           HorizontalAlignment="Right"
                           TextWrapping="Wrap"/>
                
            </StackPanel>
        </Border>

        <!-- === Bottom Controls (mic + close) === -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,0,0,40"
                    IsHitTestVisible="True">
            <Button Content="🎧"
                    Width="60" Height="60"
                    Background="#22FFFFFF"
                    BorderBrush="Transparent"
                    FontSize="26"
                    Foreground="White"
                    Click="Mic_Click"/>
            <Button Content="❌"
                    Width="60" Height="60"
                    Margin="12,0,0,0"
                    Background="#22FFFFFF"
                    BorderBrush="Transparent"
                    FontSize="24"
                    Foreground="White"
                    Click="Close_Click"/>
        </StackPanel>

        

    </Grid>
</Window>
