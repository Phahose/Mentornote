<Window x:Class="Mentornote.Desktop.Windows.ViewSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Appointment Summary"
        WindowStartupLocation="CenterOwner"
        Height="450" Width="600"
        Background="#FF2A2A2A"
        Foreground="White">

    <Grid Margin="20">

        <StackPanel>
            <TextBlock 
                       x:Name="AppointmentTitle"
                       Text=""
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="0 0 0 10"/>

            <TextBlock x:Name="AppointmentDate"
                       Text=""
                       Margin="0 0 0 5"/>

            <TextBlock x:Name="AppointmentStatus"
                       Text=""
                       Margin="0 0 0 10"/>

            <TextBlock Text="Summary:"
                       FontSize="36"
                       FontWeight="SemiBold"
                       Margin="0 15 0 5"/>

            <ScrollViewer Height="750">
                <wpf:WebView2 x:Name="MarkdownBrowser"
                          Margin="10"
                          DefaultBackgroundColor="Transparent"/>
            </ScrollViewer>
            
            
            <Button Content="Close"
                    Height="35"
                    Width="100"
                    HorizontalAlignment="Right"
                    Margin="0 15 0 0"
                    Click="Close_Click"/>
        </StackPanel>

    </Grid>
</Window>
