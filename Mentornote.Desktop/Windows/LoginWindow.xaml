<Window x:Class="Mentornote.Desktop.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Width="580" Height="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1C1C1C">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <!-- App Title -->
        <TextBlock Text="MentorNote"
                   FontSize="32"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="White"/>

        <!-- Login Fields -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" >

            <!-- Email -->
            <StackPanel Margin="0,15">
                <TextBlock Text="Email"
                           Foreground="#9E9E9E"
                           FontSize="14"/>
                <TextBox x:Name="EmailBox"
                         Height="40"
                         FontSize="16"
                         Padding="10"
                         Foreground="White"
                         Background="#2C2C2C"
                         BorderBrush="#3A3A3A"
                         BorderThickness="1"
                        />
            </StackPanel>

            <!-- Password -->
            <StackPanel>
                <TextBlock Text="Password"
                           Foreground="#9E9E9E"
                           FontSize="14"/>
                <PasswordBox x:Name="PasswordBox"
                             Height="40"
                             FontSize="16"
                             Padding="10"
                             Foreground="White"
                             Background="#2C2C2C"
                             BorderBrush="#3A3A3A"
                             BorderThickness="1"
                            />
            </StackPanel>

            <!-- Error Message -->
            <TextBlock x:Name="ErrorMessage"
                       Foreground="#FF4444"
                       FontSize="13"
                       Visibility="Collapsed"
                       TextWrapping="Wrap"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Login Button -->
        <Button Grid.Row="2"
                Content="Login"
                Height="48"
                FontSize="18"
                Foreground="White"
                Background="#3F6EF6"
                BorderThickness="0"
               
                Cursor="Hand"
                Click="LoginButton_Click"/>
    </Grid>
</Window>
